import { Component } from "@/components/ui/parallax-scroll-feature-section";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Interactive Parallax Showcase | Digital Heroes",
  description: "Experience our advanced interactive scrolling animations, custom GSAP components, and layout workflows.",
  alternates: {
    canonical: "/demo-parallax",
  },
};

export default function DemoOne() {
  return <Component />;
}
